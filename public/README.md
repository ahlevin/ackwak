# Public assets

This folder holds files that should be copied to the build output as-is (favicons, robots.txt, etc.).

For GitHub Pages with a custom domain, add a `CNAME` file here containing your domain on a single line:

```
ackwak.com
```

For Cloudflare Pages, you don't need a CNAME file, custom domains are configured in the Cloudflare dashboard.
